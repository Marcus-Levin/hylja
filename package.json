{
  "name": "hylja",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Semantic information-control layer for AI and agentic systems",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run check:fixtures && npm run build && node --test test/*.test.mjs evaluations/candidates/reference.test.mjs evaluations/preparation-integrity.test.mjs",
    "test:coverage": "npm run check:fixtures && npm run build && node --test --experimental-test-coverage test/*.test.mjs evaluations/candidates/reference.test.mjs evaluations/preparation-integrity.test.mjs",
    "check:fixtures": "node scripts/check-fixtures.mjs",
    "audit:deps": "npm audit --audit-level=high",
    "sbom": "npm sbom --sbom-format cyclonedx"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
