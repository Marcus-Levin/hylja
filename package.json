{
  "name": "hylja",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Semantic information-control layer for AI and agentic systems",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
