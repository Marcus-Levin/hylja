# #39 v0 open gates — decision packet p0.1

**Status: unanswered, non-normative preparation; NOT an approval, pre-tuning lock, blind split, scored v0, authorization, or release decision.** [Issue #39](https://github.com/Marcus-Levin/hylja/issues/39) and the [evaluation policy](../evaluation.md) govern acceptance. [The plan](../plan.md#current-state) alone records implementation status. The [public development fixture](issue-39-public-development-fixtures-p0.1.json), [independent development oracle proposal](issue-39-public-development-oracle-p0.1.json), [rubric proposal](issue-39-public-rubric-p0.1.md) and [corpus preparation](issue-39-corpus-protocol-prep-p0.1.md) supply review inputs, **not** signed destination policy or blind truth. No private examples, seeds, answers, custody locations, or hashes belong in this packet.

## Decisions requiring actual owners

Record a reviewer identity, decision, rationale, version and date **before** changing this packet's pending state. An agent's inspection of public synthetic examples is not a human policy grant or independent blind custody. If an answer changes scope, treatment, oracle or scoring after a lock, create a new version, never silently revise a scored v0.

| Gate | Owner and question to answer | Pending decision / consequence |
| --- | --- | --- |
| Supported scope | Protocol steward and evaluation owner: is the first **partial** v0 deliberately limited to D01/D02/D05 development tasks and independently executable, structurally distinct blind cases at a controlled synthetic `model.input` text/JSON capture? Or must #39's full D01–D12 inventory and H01–H04 roles be executable before v0? | **PENDING.** Narrow is recommended as a truthful early evidence gate, **not #39 closure**; reject blind families/roles the runner cannot exercise rather than calling them covered. Full inventory delays v0. Fix the supported family/surface/representation/class matrix and explicit unsupported/untested/outside-architecture rows *before* family selection or scoring. Public family IDs alone provide no coverage. |
| Destination and treatment | Independently designated policy owner/reviewer: approve each planted occurrence's expected treatment/fidelity for a real *observed controlled sink profile* and conservative unknown-route behavior, without using candidate outcomes as policy input. Decide whether D02's non-live SECRET-shaped token may be irreversibly masked while the task continues or requires terminal `BLOCK`; review partial-secret, metadata and contextual-risk gates. | **PENDING.** The DEV oracle proposes MASK for the D02 token; the [candidate README](../../evaluations/candidates/README.md#measured-limitations) documents terminal BLOCK for token-shaped evidence, while the [plan](../plan.md#current-state) records candidate availability and lock status. Neither proposal may be selected merely to match or disadvantage that candidate. Require a future runner to withhold even a controlled synthetic send on missing/mismatched evaluator-owned route observation or failed required check; the [#5 seam contract](../contracts/synthetic-evaluation-seam.md#registration-and-candidate-handoff) documents a local callback, not independent route/profile authentication or enforced policy, so a capture alone cannot prove those conditions. No `KEEP` or proposed profile grants authority. Future-scoped synthetic labels grant no current restoration, USE, DISPLAY or EXPORT. |
| Task and scoring | A **separate** engineering-task reviewer approves D01 escalation ordering, D02 HTTPS 443-versus-8443 diagnosis, and D05 valid JSON/HTTPS effective 443, `/v1/ping`, Linux output-file semantics and candidate-visible entity relationships; evaluation/scoring reviewer approves metrics and critical gates. | **PENDING.** Fix NAME/EMAIL/PHONE candidate-miss and false-cloak attribution, one-to-one UTF-8 spans/overlaps, denominator and `n/a` rules, full versus substring/encoded SECRET leakage, body **and** metadata, multi-sink repeats, hard gates, weights or explicit unweighted comparison, and blocked/held utility=unsolved. A verified zero-byte BLOCK is not task success; missing/unobserved capture is **untested**, not a no-leak pass. See the [rubric proposal](issue-39-public-rubric-p0.1.md). |
| Blind partition and custody | An **independent** synthetic blind fixture/oracle author and custodian specifies an audited access-controlled store **outside Git, PR, CI artifacts and candidate-accessible worktrees**, separate access principals, retention and review. Approve structural independence, scope compatibility, negative controls, integrity/version disclosures and offline/sandboxed candidate execution without oracle-store access or uncontrolled output/network; a callback API alone is not an isolation boundary. Keep payloads/labels/answers unavailable to candidate authors. | **PENDING.** Merely creating a separate worktree under the same Unix user does not establish access separation. H01–H04 are public planning IDs, not stored cases, evidence or an automatically supported role. Without genuine custody and runnable cases, no blind score, complete v0 lock or #39 closure may be claimed. |
| Chronology and reference eligibility | Protocol steward and independent reviewer decide how to record the already exposed public reference candidate against #39's requirement to freeze treatment/fidelity **before candidate tuning**. | **PENDING.** [Issue #39](https://github.com/Marcus-Levin/hylja/issues/39) unconditionally requires expected treatment/fidelity frozen **before candidate tuning**, yet the [candidate's documented development history](../../evaluations/candidates/README.md#measured-limitations) and [plan](../plan.md#current-state) record exposed D01/D02/D05 use before a formal policy/task/rubric lock. Its oracle-free design and absence of scores do **not** satisfy that historical prerequisite. Label this version exploratory DEV, disclose its commit/exposure, stop detector tuning and do not represent it as an eligible untuned #39 v0 baseline. For the original acceptance criterion, prospectively lock an independently approved policy/task/rubric/blind protocol **before** authoring/tuning a fresh eligible baseline; alternatively the human sponsor must formally change the issue criterion and record the resulting eligibility rule **before** scoring, without claiming the original historical gate was met. Snapshot any candidate/config before separately custodied blind execution. Never revise D02 oracle to fit its BLOCK. |

## Preparatory work permitted without these answers

Public synthetic projection, bounded candidate callback, evaluator-owned **local** route observation, final serialized body/metadata capture, no-send denial tests, independently authored test-only planted controls and engineering predicates can be implemented and reviewed as **unscored development smoke**. It is not authenticated production routing, actual external transport interception, a #19 final-byte sentinel, vault/broker isolation or a scored blind comparison. The [#5 seam contract](../contracts/synthetic-evaluation-seam.md#registration-and-candidate-handoff) specifies that an expected sink without captured bytes is `untested`, and that the callback does not establish authenticated routing. Separate D05 JSON leaf-to-serialized-UTF-8 span attribution and blind partition support must be independently implemented and checked before using the public oracle for scored runs.

Only after all owners sign can a protocol steward integrity-record a **prospective pre-tuning lock before authoring/tuning a fresh eligible baseline** (or the formally changed issue criterion), followed by a separate executable final-v0 freeze before comparisons: declared scope/exclusions, public and restricted corpus/split, independently reviewed oracle, destination/policy/fidelity, task prompts/answers/rubric, scoring/abstention/critical gates, runner/candidate/config exact versions and controlled serialized-byte evidence. Retain restricted content and any sensitive integrity disclosures solely under the approved custodian. Publish allowlisted counters, opaque refs, applicability/limitations and reproducibility instructions; keep scored [#40](https://github.com/Marcus-Levin/hylja/issues/40)–[#45](https://github.com/Marcus-Levin/hylja/issues/45) and [#47](https://github.com/Marcus-Levin/hylja/issues/47) candidate comparisons and [#48](https://github.com/Marcus-Levin/hylja/issues/48) selection outside this preparation.
