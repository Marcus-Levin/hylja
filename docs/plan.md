# Development plan

This plan is the single authoritative home for implementation status. Architecture and specifications describe accepted behavior; they do not prove implementation.

## Current state

**Foundation issues [#1](https://github.com/Marcus-Levin/hylja/issues/1)–[#5](https://github.com/Marcus-Levin/hylja/issues/5) are closed as completed at their bounded, NON-ENFORCING scope.** Their independently reviewed implementations landed in [PR #51](https://github.com/Marcus-Levin/hylja/pull/51), [#52](https://github.com/Marcus-Levin/hylja/pull/52), [#53](https://github.com/Marcus-Levin/hylja/pull/53), [#56](https://github.com/Marcus-Levin/hylja/pull/56) and [#58](https://github.com/Marcus-Levin/hylja/pull/58); the public unscored #39 adapter landed separately in [PR #60](https://github.com/Marcus-Levin/hylja/pull/60). [Main CI run 36169013008](https://github.com/Marcus-Levin/hylja/actions/runs/36169013008) passed the fixture guard, locked build/typecheck/tests/coverage, dependency audit and ephemeral SBOM after those merges. Earlier local/stacked PR checkpoints below retain their historical timing, not the current merge/approval state. Product direction and the backlog remain foundation-only. The TypeScript source still contains no identity authenticator, production adapter/broker, runtime-enforced Policy Engine, vault, egress sentinel, transport interceptor or privacy boundary; #39 remains open without independent blind custody, approved destination/task rubric, a pre-tuning or final-v0 lock or a scored baseline.

Local #1 evidence (Node 22.22.3, npm 11.15.0): the red command `node --test test/*.test.mjs` failed with 0/6 passing before the runner/guard existed; its deliberately throwing runtime probe was ignored by the old typecheck-only `npm test` (which exited 0 with dependencies installed). After implementation, locked `npm ci --ignore-scripts --no-fund`, `npm run build`, `npm run typecheck`, `npm test`, and `npm run test:coverage` passed; disposable synthetic negative cases reject force-added ignored private fixture paths, tracked credential-like fixture blobs, generated artifacts, binary/symlinked fixtures, and guard execution from a subdirectory. The designated synthetic golden fixture is accepted. The Node test runner reports line/branch coverage for this small toolchain and compiled module, not coverage of future enforcement behavior. `npm run audit:deps` reported zero known vulnerabilities locally; `npm run --silent sbom` produced parseable CycloneDX JSON. CI is configured to run these gates from the lockfile without installing lifecycle scripts or persisting SBOM artifacts; both [#1 PR #51 check run 36134297661](https://github.com/Marcus-Levin/hylja/actions/runs/36134297661) and [run 36134323695](https://github.com/Marcus-Levin/hylja/actions/runs/36134323695) completed successfully.

Threat coverage for #1 is limited to supply-chain hygiene (locked integrity-pinned dependency, local audit/SBOM, pinned CI actions) and accidental tracked-fixture/CI-output disclosure (Git-index guard, ignored private paths, no planted value in guard output). New surfaces are npm installation, the Git-backed guard and test runner, and CI network/runner output. The guard uses bounded deterministic patterns, **not** exhaustive secret/PII detection; encoded values, contextual identifiers, and non-fixture files still need human review and future controls. No customer/production data or semantic/policy behavior is involved, so downstream utility, model latency/cost, held-out and shadow comparisons are not applicable to this toolchain slice.

Local #2 evidence (Node 22.22.3, npm 11.15.0; based on reviewed #1 head `39f11f1`): after locked `npm ci --ignore-scripts --no-fund`, baseline `npm test` exited 0 (10/10). Nine public behavior tests were written first and the existing `npm test` runner then exited 1 with 10 pass / 9 fail against compileable unimplemented envelope exports (fixture guard/build passed; no missing dependency). Security regression tests for non-echoing errors and bounded UTF-8/branching input also failed before their fixes (21 pass / 2 fail). After initial implementation, a clean locked `npm ci --ignore-scripts --no-fund`, `npm run build`, `npm run typecheck`, `npm test` and `npm run test:coverage` all exited 0; tests reported 23/23 passing, with the envelope compiled module at 98.77% line, 86.10% branch and 100% function coverage. These numbers are test coverage of a schema/binding module, **not** release safety or enforcement coverage; at this initial local checkpoint remote CI had not yet run. Final #2 review and remote CI evidence are recorded below.

#2 synthetic golden/regression cases exercise exact JSON round trips for all 26 initial operation families, unknown/unversioned/malformed rejection, four fresh ordered proof claims against separately supplied identity/request/source/actual-route context, forged/stale/missing provenance, mismatch across principal/workload/tenant/project/session/purpose/source/destination/profile/redirect, payload and metadata `CONTROL` claims, stream continuity/cancellation, malformed representation, immutable payloads, non-echoing errors, and UTF-8/traversal limits. A deterministic 64-case tenant/project/principal/route variation set checks each mismatch against an otherwise identical, positively parseable binding (stable proof timestamps); no protected release permission is produced. This addresses untrusted-content promotion, poisoned model/tool claims, tenant/confused-deputy mixups, route/profile substitution and a limited fragment-context aspect of streaming leaks. The new attack surface is the adapter-to-core trusted-context handoff and its proof/clock/redirect lifecycle: #2 checks congruence/freshness only, **not** authentication or cryptographic proof integrity. Upstream adapters/brokers must authenticate principals/workloads, verify project membership, observe effective routes including redirects, guard the context argument, and track stream history; a caller able to fabricate that argument can fabricate authority. A replayed first fragment within the freshness window or an unobserved redirect remains possible without later stateful enforcement; this module neither decodes representations nor inspects data or transmits bytes. Policy authorization, secret handling, brokered uncloak, holdback and independent exact-byte egress checks remain future work. No real/private corpus was used; synthetic in-memory examples are all reserved `.invalid`/`example.invalid` values. Utility evidence is exact payload/schema round trip, **not** downstream task success or fidelity under cloaking. Informational local 1,000-cycle synthetic create/serialize/parse measurements were 177.34 ms before review fixes and 248.23 ms after (~0.248 ms/cycle after); these are neither a traffic-latency SLA nor a benchmark versus #1, which had no envelope. No model/semantic calls or external egress occur, so model latency/cost and actual privacy-release metrics are not applicable.

Independent #2 review found and reproduced four further contract bugs on the first commit: an 80,000-key sub-1-MiB wire took ~8.27 s to reject due to quadratic key checks; a time-varying JavaScript adapter object could pass validation and bind a different operation/stream flag; a forged internal error instance could echo planted data; and an event within the five-minute freshness window could predate all four proof issue times. Public-behavior regressions were added first (`npm test` exit 1, 23 pass / 4 fail); a synthetic local-sink positive round trip and missing-local-profile create/parse negatives were also added. The corrected snapshot-based schema reads each permitted data descriptor once, caps object keys at 10,000 before per-key inspection, validates in linear time, returns generic non-echoing errors, and requires `occurredAt` within each proof interval. After these fixes, locked `npm ci --ignore-scripts --no-fund`, `npm run build`, `npm run typecheck`, `npm test`, `npm run test:coverage`, and `git diff --check` all exited 0; 27/27 tests passed, and compiled envelope coverage was 98.85% lines / 85.79% branches / 100% functions. The near-limit-plus-80,000-key regression took ~0.30 s locally after the fix (timing threshold 4 s; not a service SLA). At that intermediate #2 checkpoint, remote CI and independent rereview were still outstanding. Neither the context object nor its timestamped proof references authenticate themselves: actual independent identity/session and project binding, effective route/profile and redirect observation, and deny-before-bytes conformance are pending [model gateway #21](https://github.com/Marcus-Levin/hylja/issues/21) and [MCP gateway #23](https://github.com/Marcus-Levin/hylja/issues/23). The standard parser currently collapses duplicate raw JSON keys last-wins; this is a differential-parser residual, not permission to release. Downstream integrations must use the normalized bound envelope/serializer rather than forwarding raw wire to another parser, and prove that at their own protected release boundary (or reject duplicates before use). No release, authentication or egress enforcement is claimed from #2.

A further independent rereview at `4546174` found that a time-varying JavaScript Proxy could pass the first small-key cap, then trigger a second uncapped `ownKeys` enumeration inside whole-object descriptor collection. A new synthetic public-behavior regression first failed (`npm test` exit 1, 27 pass / 1 fail) when a four-key draft expanded to 80,000 reported keys on the second enumeration; it also checks that an over-cap first enumeration causes zero descriptor visits. The fix inspects descriptors **only for the first captured capped key list** and rejects missing/malformed descriptors without re-enumerating the caller object. A clean locked `npm ci --ignore-scripts --no-fund`, `npm run build`, `npm run typecheck`, `npm test`, `npm run test:coverage`, and `git diff --check` then all exited 0; 28/28 tests passed, with compiled envelope coverage at 98.84% lines / 85.79% branches / 100% functions. This is bounded behavior for the tested synthetic proxy, not immunity from arbitrary host-level resource exhaustion. At that intermediate #2 head, remote CI and independent rereview were still outstanding; no protected-release claim follows from the fix.

Final #2 head `9b5b80a` was independently approved after the rereview. On [#2 PR #52](https://github.com/Marcus-Levin/hylja/pull/52), the `foundation` checks completed successfully for both [push run 36138924023](https://github.com/Marcus-Levin/hylja/actions/runs/36138924023) and [PR run 36138960006](https://github.com/Marcus-Levin/hylja/actions/runs/36138960006); `gh pr checks 52 --watch` exited 0. These are CI results for #2, not for #3 or an enforcement boundary; PR opening/check success does not establish merge status.

Local #3 evidence (Node 22.22.3, npm 11.15.0): on the original provisional #2 head `9a2b794`, a clean locked `npm ci --ignore-scripts --no-fund` and baseline `npm test` exited 0 (23/23). Public classification behavior tests were written before implementation; with compileable unimplemented exports, `npm test` exited 1 (23 pass / 12 behavioral assertion failures, with fixture guard and build passing), now retained as the rebased red checkpoint `fa6637f`. A separate synthetic chained-registry growth regression also failed before its fix (`node --test test/classification.test.mjs` exit 1, 12 pass / 1 fail). After implementation, rebase onto reviewed #2 head `9b5b80a`, and the bounded-registry fix, a clean locked `npm ci --ignore-scripts --no-fund`, `npm run build`, `npm run typecheck`, `npm test`, `npm run test:coverage`, and `git diff --check` all exited 0: 41/41 tests pass, including 13 classification test groups and 28 inherited foundation/envelope tests. Compiled classification coverage is 94.66% lines / 90.48% branches / 100% functions. This measures local contract exercise, **not** detector recall, authorization, protected-egress safety, or production coverage. The final #3 head `e3b2ef6` was independently approved after review. On [#3 PR #53](https://github.com/Marcus-Levin/hylja/pull/53), the `foundation` checks completed successfully for both [push run 36139816058](https://github.com/Marcus-Levin/hylja/actions/runs/36139816058) and [PR run 36139854588](https://github.com/Marcus-Levin/hylja/actions/runs/36139854588); those checks do not establish merge status or a protected-egress boundary.

#3 synthetic tests cover all 12 initial classes, documented initial subtypes plus bounded per-class extensions, sensitivity separate from source trust, versioned producer/input/question-set/model provenance, frozen copied records, SECRET/credential default non-reversibility and highest claimed sensitivity despite semantic `PUBLIC`/forged `KEEP`, explicit unresolved missing/unknown/unsupported/conflicting/abstained/failed evidence, and 128 deterministic permutations of detector/parser/semantic arrival order. Negative cases include forged payload/semantic `CONTROL`, forged detector origin in semantic evidence, extra action/authorization fields, duplicate IDs, parser/classifier outages, malformed/proxy/accessor metadata and time-varying array length, and cumulative subtype-registry growth beyond 128 entries per class. These address classifier false-negative downgrade, untrusted-content promotion, last-writer-wins conflicts and bounded-input/registry exhaustion within the tested surface. New attack surfaces are the trusted-integration-to-composer evidence channels, separately supplied source context, versioned provenance strings, and extensible subtype registry. The pure composer neither authenticates its caller/context/evidence nor recognizes protected spans, grants effects, creates mappings, invokes a judge, applies policy, checks final egress bytes, or logs traffic. Upstream integration must authenticate/bind evidence and trust to the actual interaction and assign privacy-safe opaque IDs/references: valid provenance text can echo a planted original if the integration supplies one, so this composer does **not** sanitize provenance by itself. UNKNOWN/unresolved results require the separate #4 deterministic policy seam and later enforced integration; no release claim follows from #3 tests. All inputs are synthetic `.invalid` identifiers; no real/private corpus, held-out semantic/model behavior, shadow comparisons, downstream task utility, or external judge request is evaluated in #3.

Local #4 evidence (Node 22.22.3, npm 11.15.0): on provisional #3 head `7a235a9`, locked `npm ci --ignore-scripts --no-fund` and baseline `npm test` exited 0 (35/35). Public policy behavior tests and a compileable denying stub were committed first; `npm test` exited 1 (36 pass / 12 fail, with build and fixture guard passing), retained after rebase as red checkpoint `3f9134f` (independent replay against the rebased denying stub: 42 pass / 12 fail). Separate synthetic same-ID/version policy-substitution and bounded Proxy reflection regressions failed in an **author-observed focused run against the uncommitted first implementation** (`node --test test/policy.test.mjs` exit 1, 17 pass / 3 fail); test commit `e1800aa` independently replayed against the denying stub at 43 pass / 18 fail, but its substitution assertion could pass vacuously on that stub. The green implementation includes an explicit `BUNDLE_MISMATCH` assertion, not a claim that the committed red stub proved this particular behavior. After the implementation commit `cfbf150` and rebase onto independently reviewed #3 head `e3b2ef6` (built on reviewed #2 head `9b5b80a`), clean locked `npm ci --ignore-scripts --no-fund`, `npm run build`, `npm run typecheck`, `npm test`, `npm run test:coverage`, and `git diff --check` exited 0: 61/61 tests pass, including 20 policy groups and 41 inherited foundation/envelope/classification tests. Compiled policy coverage is 97.18% lines / 89.58% branches / 100% functions; this measures deterministic policy choices, **not** actual protected-byte release safety. The first independent #4 review at `f33ab6f` found two blocking P1 defects described below; no #4 approval, push, PR or remote CI is claimed.

#4 synthetic cases exercise 180 class/source-trust/actual-sink combinations (12 classes × five trusts × three sinks), a five-sensitivity matrix, separate SEND/USE/DISPLAY/EXPORT rules, and 64 tenant-scoped pending-review variations. Positive controls precede negative decisions: missing/unknown/mismatched source, authenticated principal/workload/tenant/project/session/purpose, actual route/profile/redirect, bundle ID/version or independently pinned content digest, malformed configuration, unresolved/UNKNOWN or failed classification, semantic `KEEP` against explicit `BLOCK` or a cleartext ceiling, external credential/SECRET unsafe treatments, and missing/forged/stale review outcomes deny or remain held. Deterministic replay uses a versioned trusted policy snapshot and privacy-safe opaque references; `REQUIRE_REVIEW` holds without bytes/effects until a separate, upstream-authenticated review outcome selects a currently permitted treatment. This addresses tested policy-substitution/confused-deputy release, semantic downgrade and review-transition threats; new surfaces are trusted bundle/profile resolution, digest pinning and review handoff. The pure module **does not authenticate its caller, independently verify identity/project membership/route/policy distribution/reviewer approval, transform content, issue broker grants, send bytes or perform final-byte egress checks**: upstream integrations must supply and protect those facts; a caller able to fabricate both the trusted context and bundle digest can fabricate a policy decision. The digest and decision fingerprint are unkeyed replay commitments, not authorization tokens. No actual outbound payload, vault isolation, end-to-end leakage, downstream utility, held-out semantic cases, shadow comparison or production data were evaluated in #4. A selected treatment cannot itself release data; downstream transformation and independent exact-byte enforcement remain later work.

First independent #4 review reproduced two **blocking P1** counterexamples at `f33ab6f`: different PERSON/RESTRICTED candidate units A/B in one interaction produced identical held decision fingerprints, so an approval for A selected a treatment for B; a forged `RESOLVED PERSON/PUBLIC` record with empty evidence and the same provenance replaced a trusted RESTRICTED classification and selected external `KEEP`. A separate compiling-schema red checkpoint `ff584b8` added five direct synthetic assertions: `npm test` exited 1 (61 pass / 5 fail) with the existing suite still green; failures specifically demonstrated both substitutions, empty resolved evidence and a request-side candidate mismatch. After fix `875b1bd`, clean locked `npm ci --ignore-scripts --no-fund`, `npm run build`, `npm run typecheck`, `npm test`, `npm run test:coverage`, and `git diff --check` exited 0: 67/67 tests pass, including 26 policy groups and 41 inherited groups. Compiled policy coverage is 96.70% lines / 84.39% branches / 100% functions. Positive same-candidate/same-composer controls and 64 distinct candidate variations show the actual behavior: a separately supplied per-unit opaque candidate ID and independently pinned SHA-256 of the full normalized classification must match before selection; review fingerprints and separately supplied outcomes bind both, so an A approval remains held for B. A `RESOLVED` record without nonempty, structurally consistent detector `FOUND` evidence is rejected. This is still **congruence, not authentication**: the trusted integration must assign unique privacy-safe IDs per classified unit, authenticate the composer handoff and pin its digest independently of request content, and independently authorize reviewers. Classification provenance and candidate IDs must not contain originals; the digest/fingerprint are not bearer credentials. No bytes are emitted by these tests or this seam. Independent #4 rereview of the new head remains required; no push, PR, remote CI or acceptance is claimed.

The #4 review-pending wording above records an earlier checkpoint. Subsequently its final `91204a5` head was independently approved and both `foundation` checks on stacked [PR #56](https://github.com/Marcus-Levin/hylja/pull/56) completed successfully (PR remains open at this local #5 checkpoint). This approval concerns the pure policy decision seam only, not authentication, runtime authorization or byte-level release.

Local #5 evidence (Node 22.22.3, npm 11.15.0): before the generic seam, locked-install `npm test` passed the inherited 41/41 suite. Compileable stubs and behavioral tests first failed 41 pass / 16 fail at the original red checkpoint `ce20afe` (rebased checkpoint `ef03dd6`); initial green generic seam was 62/62 on provisional #3. Independent #5 review found four synthetic defects: a contradictory credential oracle omitted SECRET from the escape denominator; a throwing scope-boundary getter echoed a planted error; changing-length Proxy fixture arrays bypassed a bound; caller-authored report IDs exposed a protected prefix. Separate red test checkpoints proved these behaviors before fixes; the revised generic seam was 67/67 on #3 and, after rebase onto reviewed #4, clean locked baseline was 93/93 at `165279d`. The real #4-dependent synthetic policy adapter and test matrix were added after the compileable `edd7766` red checkpoint (`npm test` exit 1, 93 pass / four behavioral failures, with build/fixture guard passing). After implementation, clean locked install/build/typecheck/test/coverage and `git diff --check` pass 97/97 tests; compiled `synthetic-policy-evaluation.js` coverage is 97.10% lines / 91.67% branches / 100% functions, with `evaluation.js` 99.58% / 85.19% / 98.59% and `synthetic-scope-contract.js` 99.47% / 84.15% / 100%. These numbers measure synthetic contract tests, **not** production enforcement or real tenant isolation. The #5 final head still requires independent review; no #5 push/PR/remote CI is claimed.

The #5 review/push/CI-pending wording above records an earlier local checkpoint. Subsequently the exact implementation head `fcef6e5` was independently approved (97/97 tests; 54 real policy checks, 50 permissive failures), and both `foundation` checks on open stacked [PR #58](https://github.com/Marcus-Levin/hylja/pull/58) completed successfully: [push run 36145715465](https://github.com/Marcus-Levin/hylja/actions/runs/36145715465) and [PR run 36145756698](https://github.com/Marcus-Levin/hylja/actions/runs/36145756698). Approval and CI do **not** establish production enforcement, vault/broker isolation, actual egress safety or a frozen/scored #39 protocol; this documentation-only follow-up needs its own narrow review and CI.

#5's independent planted oracle and controlled in-memory serialized body/metadata capture measure exact SECRET plaintext escapes separately from candidate recognition, downstream task correctness and untested sinks; sanitized ordinary reports mint non-source-derived case/sink refs. Public development/blind family IDs are disjoint, but blind payloads/labels are not in Git and no #39 comparative protocol is frozen or scored. A/B property-style scope vectors and a strict synthetic double pass while intentionally permissive scope and policy doubles fail many denial/review checks. The integrated #4 cases exercise actual pure SEND/USE policy choices, missing/mismatched route/profile/pinned bundle/composed classification, denied/held/review states, tenant/project/session/provenance and per-candidate A/B review replay; local USE selection does **not** grant DISPLAY, EXPORT or cross-tenant resolution through a separate synthetic authorization double. No fixture authenticates a real principal, reviewer, policy pin, project membership or route, implements a vault/key/cache/broker, transforms plaintext, contacts a sink or proves a final-byte sentinel. #7 parser exhaustion, #8/#19 byte release, real scoped storage/authorization, end-to-end utility and held-out independent evaluation remain pending. The concrete threat-to-test map and these limits are in [the #5 seam contract](contracts/synthetic-evaluation-seam.md).

A separate [deterministic/no-Jev reference candidate](../evaluations/candidates/README.md) is available for public synthetic development inputs only. It is oracle-free, evaluation-only and NON-ENFORCING: its `TRANSFORMED` output is not safe-to-send, and no #39 blind split, pre-tuning/final-v0 lock, scored baseline or independently checked real egress is claimed.

The focused [public #39 development smoke adapter](../evaluations/README.md) now bridges that unchanged reference to #5 `runCandidate` and the controlled in-memory body/metadata capture for D01 text, individual D02 log lines and canonical D05 JSON. Its RED checkpoint `9721e13` passed fixture guard/build but failed 9/10 focused behavioral tests against compileable stubs (one negative passed); the GREEN implementation passed a clean locked `npm ci --ignore-scripts --no-fund`, build, typecheck, `npm test`, coverage and `git diff --check` locally: 124/124 tests, including the existing 15 baseline candidate groups newly included in both npm test scripts and the 12 adapter groups. The adapter module measured 100% lines / 78.98% branches / 97.06% functions under Node's test coverage; these are local contract-branch counts, **not** safety, interception or release coverage. Existing CI runs both updated scripts, but no remote CI execution, push, PR or independent final-head approval is claimed for this worktree. Synthetic tests check evaluator-minted refs, UTF-8 NAME/EMAIL/PHONE spans with an astral prefix, D02 `BLOCK` zero bytes and unsolved utility, D05 candidate-visible task edit/validity, exact controlled serialized body and metadata, independent test-specific seeded leaks in either captured channel, and missing-route/serializer/event/representation restrictive behavior. A missing capture is untested, not a safety pass; D02 `BLOCK` is not evidence of agreement with a proposed destination-specific irreversible `MASK`. No existing independent public oracle labels/values/answers or held-out content were read or used for candidate design/tuning: D05 decoded JSON leaf-to-serialized-UTF-8 oracle span projection is pending and the original oracle is **not** scored here. The reference predates a formal #39 treatment/task/rubric pre-tuning lock, so these exploratory DEV observations cannot retroactively establish one, scored v0 eligibility, an actual route/#19 sentinel, enforcement or any #40–#47 comparison. Snapshot the existing candidate head and obtain the separately reviewed baseline before future blind scoring; current local results do not authorize any traffic.

The first independent exact-head security review **blocked `d89f62f`**: an injected D05 `TRANSFORMED` result with malformed JSON field or task response was captured before any representation check, and a changing top-level oracle Proxy could make the adapter believe a task answer existed while #5 registered an oracle without one, then reject only **after** a controlled capture. Separately, 256 repeated events over a 65,536-byte field rebuilt the same UTF-8 boundaries per event, taking ~12.476 s in one focused local run. Test-only RED commits `3b035c7` (12 pass / two behavioral failures) and `2c7c905` (12 pass / three failures) established the pre-send bugs; `3cf2ea5` added a passing, deliberately non-timing-gated bounded-event acceptance regression before caching. The correction snapshots the top-level evaluator oracle once and passes that same copy to #5, validates D05 transformed JSON object syntax and optional response's public config object field types before send (an exploratory DEV-only structural check, **not** an approved destination/task rubric, complete duplicate-key fidelity or task grade), and computes UTF-8 boundary sets once per projected field. The focused 256-event local run changed from ~12.476 s to ~0.165 s wall time on this host; this is a bounded-work regression observation, **not** an operational SLA. Clean locked `npm ci --ignore-scripts --no-fund`, build, typecheck, `npm test`, coverage and `git diff --check` pass locally at the corrected checkpoint (128/128 test groups, including the 15 existing candidate groups and 16 adapter groups). Independent rereview of the corrected exact head remains pending; no push, PR, remote CI, oracle scoring or safe-egress claim follows from local green results.

The earlier D05 projection-pending wording above records its adapter checkpoint. A further focused public D05 source→canonical field-span projector is implemented under [`evaluations/d05-canonical-json-spans.mjs`](../evaluations/d05-canonical-json-spans.mjs) as evaluator-private, synthetic-only and NON-ENFORCING. Its compileable RED stub/tests at `8c4c789` passed the fixture guard/build but failed five of nine focused behavior groups (four negative stub rejections passed). Subsequent adversarial tests extended that fixture and URL grammar matrix without reading a planted oracle. A strict bounded parser now walks the **entire raw public fixture bytes**, checks unique decoded JSON keys including escaped aliases before any lossy parse, binds one `D05-DEV-001` at `/fixtures/2/input/value`, and reconstructs the pinned adapter's sorted canonical field bytes. Key/token-attached boundaries translate independently test-authored decoded leaf code-point spans to exact `field-0` UTF-8 byte slices for customer, project, endpoint **host only**, and outputPath. Tests cover repeated identical values, reordered raw keys, exact #5 test-only registration and rejection of malformed, missing, ambiguous or mutated input. It is deliberately ASCII/unescaped/flat/eight-field-only; unsupported escaped/Unicode/nested/array/integer-like, bad numeric/URL or absent raw provenance receives generic rejection, not a scored pass. The evaluator keeps decoded source truth private; the projector itself never loads official oracle data, scores, approves treatments, edits the candidate, creates blind cases or freezes v0. This locally reconstructed byte contract checks source-to-canonical attribution only, **not** fixture-file authenticity against a dishonest caller (which would require a separate trusted custodian/pinned file digest before scored use), candidate-output JSON duplicate fidelity, equivalent encoded leakage, actual sink enforcement, policy authority, scored #39 eligibility or the independent #19 sentinel. The existing pre-lock no-Jev reference remains exploratory and ineligible. The precise supported/unsupported interface is in [the evaluation adapter README](../evaluations/README.md#evaluator-private-d05-raw-source-span-interface-development-only). Local green evidence after the additional adversarial controls and merge of latest `origin/main` (`581caa9`) is the clean locked `npm ci --ignore-scripts --no-fund`, `npm run build`, `npm run typecheck`, `npm test`, `npm run test:coverage` and `git diff --check` on the final head; this is contract test evidence, not protected-release coverage. Independent exact-head security review remains required; no push, PR or remote CI for this projector is claimed. The subsequent main CI run for the separate documentation-only merge did not start jobs due to account billing/spending limits, not a test failure; do not infer a projector CI result from it.

An intermediate independent review of D05 projector head `191502a` identified a bounded **P2 attribution risk**: a sourceSpan covering only part of the `customer`, `project` or `outputPath` decoded string was accepted when its substring and normalization were internally consistent, despite all three fixed public DEV occurrences being full leaves. The same narrow URL grammar accepted a network-path-like doubled slash inside the endpoint route. The test-only RED checkpoint `56088c3` passed fixture guard/build and failed both new focused behavior groups (9 pass / 2 fail). The correction requires whole-leaf boundaries for those three controls while retaining URL host-only boundaries, and rejects empty/dot/double-slash route segments at projector creation. These are fixture-occurrence attribution limits, not approval of endpoint safety, a candidate rule or scored eligibility; independent final-head rereview remains required.

Implementation work is tracked from [roadmap issue #36](https://github.com/Marcus-Levin/hylja/issues/36), with scoped implementation issues #1-#35 and #37 carrying their own dependencies and acceptance criteria. A separate [comparative reuse/buy/build campaign #38](https://github.com/Marcus-Levin/hylja/issues/38) may propose later scope changes; it does not itself prove implementation or alter accepted security invariants.

## Dependency-ordered milestones

### 0. Foundation and executable contracts

Goal: establish TypeScript toolchain, normalized interaction envelope, classification model, policy vocabulary, deterministic security invariants, and synthetic eval harness.

Exit evidence:

- strict TypeScript build and tests;
- synthetic fixtures only;
- runnable synthetic contract/property tests for tenant-scoped policy/authorization, blocked-secret behavior, forged policy context, and fail-closed decisions (no passing placeholders); storage, cache, and broker isolation are proved again when those components exist;
- threat cases mapped to specific negative tests, including classification conflict and missing destination/profile context.

Primary issues: #1-#5.

### 1. Text cloaking proof

Goal: local synthetic text request -> deterministic and configured candidate generation -> optional local or independently guarded semantic shadow judgment -> policy -> transformation -> independent check of the exact serialized egress.

Initial categories: PERSON (including EMAIL/PHONE), CREDENTIAL_OR_SECRET, HOST, IP/URL, CUSTOMER/PROJECT. PERSON/EMAIL/PHONE must have an explicit candidate source and synthetic acceptance cases; shadow judgment is not that source.

No production vault yet beyond minimal development fixture state. No unverified hosted semantic request, opaque protected content, or unchecked stream is silently released; this slice is not a production traffic claim.

Primary issues: #6-#13, #19, and #37 (PERSON/EMAIL/PHONE candidate source).

### 2. Reversible identity and vault

Goal: encrypted scoped mappings, entity consistency, brokered USE/DISPLAY operations, lifecycle/retention, and exact authorized round-trip restoration. No production original is persisted before encryption; privacy-safe audit and unconditional tenant isolation are prerequisites for brokered reveal.

Primary issues: #14-#18, with the audit prerequisite in #20.

### 3. Model gateway proof

Goal: OpenAI-compatible local gateway using the same core contracts, authenticated caller/destination binding, streaming policy, inbound/outbound enforcement, destination profiles, and audit evidence. #21 first proves conservative inbound inspection without uncloak; #22 adds authorized response restoration. Unsupported protected formats and hosted-tool bypasses are denied or explicitly outside supported coverage, never silently forwarded.

Primary issues: #20-#22; full bidirectional restoration is the collective exit, not #21 alone.

### 4. Agent surfaces

Goal: MCP request/result protection, file and shell boundaries, CLI harness launcher, then additional provider/harness adapters against shared conformance tests.

Primary issues: #23-#26.

### 5. Enterprise hardening

Goal: workload identity, KMS/HSM operations, full multi-tenant hardening, tamper-evident audit, behavior signals, backup/recovery, network bypass resistance, policy exceptions, and incident response. Baseline tenant isolation, authentication and audit required by earlier slices are not deferred to this milestone; complete managed-egress claims wait for #32.

Primary issues: #27-#33.

### 6. Semantic long tail

Goal: extend earlier candidate sources and shadow judgment into evaluated contextual re-identification risk, deeper business/engineering classification, multimodal/nested content, and broader enterprise DLP/industrial information protection. Earlier #10-#12 work does not by itself authorize production re-identification claims.

Primary issues: #12 and #34, building on #10-#11.

### 7. Deployment profiles

Goal: package and verify local, customer VPC, on-premises and air-gapped capabilities against the boundaries proved above.

Primary issue: #35.

## Sequencing rule

A later integration does not bypass an unproved earlier boundary. For example, do not add five harness adapters before one gateway has proved normalization, classification, policy, egress sentinel, and end-to-end utility against the shared eval suite.

Foundation issues #1-#5 and the comparative campaign #38 may proceed in parallel. Freeze #39's synthetic comparison protocol against a minimal executable #5 evaluation seam before scored candidate comparisons. A disposable reference baseline is not an enforcement runtime or a reason to complete bespoke #6-#26 first.

Obtain the reviewed per-subsystem #48 reuse/wrap/buy/build/defer decision before committing to broad bespoke detector, proxy, vault, or adapter implementation. Unavailable critical evidence yields an explicitly unverified/deferred role, not an inferred security pass.

This evidence gate does not weaken the accepted contracts, Policy Engine, broker, or final egress boundary. Changes to implementation scope require explicit review before adoption; changes to accepted architectural direction also require a new decision record.

Each implementation issue states dependencies and acceptance criteria. The shared security/evaluation requirements in roadmap issue #36 apply to all implementation work.
