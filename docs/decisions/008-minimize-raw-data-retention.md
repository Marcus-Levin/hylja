# Decision 008: Raw traffic is ephemeral by default

Status: accepted foundation direction.

Raw prompts, logs, configuration files, tool results, and model responses are not persisted merely because Hylja processed them. Persist only required mappings, privacy-safe metadata, explicitly approved restricted eval cases, and durable policy/audit evidence.
