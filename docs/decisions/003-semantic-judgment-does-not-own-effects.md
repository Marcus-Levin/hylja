# Decision 003: Semantic judgment does not own effects

Status: accepted foundation direction.

Jev or another semantic service may classify, score, rank, or estimate re-identification/utility signals. Deterministic Hylja code owns authorization, policy composition, transformation, release, and side effects.

Semantic-service failure cannot downgrade a protected flow to plaintext allow.
