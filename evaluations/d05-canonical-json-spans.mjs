// Evaluator-private public D05 source -> #5 canonical JSON byte spans.
// SYNTHETIC-ONLY, NON-ENFORCING, unscored development contract.
export const D05_CANONICAL_JSON_SPAN_VERSION = 'd05-canonical-json-v1';

/** Requires actual bounded raw fixture-file bytes; never infer raw provenance from parsed objects. */
export function createPublicD05SpanProjector(_options) {
  throw new TypeError('Invalid public D05 span projection input');
}
