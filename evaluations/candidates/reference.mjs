// Evaluation-only synthetic reference candidate; deliberately incomplete TDD stub.
// The future #5 runner owns independent oracle, policy, serialization and capture.
export function runReferenceCandidate(_candidateInput) {
  return { disposition: "BLOCK", events: [], reason: "NOT_IMPLEMENTED" };
}
